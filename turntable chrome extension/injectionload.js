document.body.appendChild(document.createElement('script')).id='autoawesome'; 
document.getElementById('autoawesome').src=chrome.extension.getURL('autoawesome.js');  //add a script element to the DOM (it gets executed too)
